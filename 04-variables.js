/*
        VARIABLES
    Las variables se definen:
      · Deben de comenzar con una letra, un guión bajo o un signo de dolar.
      · Permite letras, tanto mayúsculas como minúsculas.
      · Permite números.
      · Se utiliza camelCase
*/

/*
        VAR - LET
    var: declarar variables globales (NO RECOMENDADO).
    let: declarar variables globales y de bloque (USAR SIEMPRE).
*/

let variable= "Esto es ua variable";
console.log(variable);